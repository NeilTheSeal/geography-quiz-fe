<div id="user-dashboard">

  <div id="dashboard-header">
    <div id="dashboard-name"><%= @user.name %>'s Dashboard</div>
    <div id="dashboard-buttons">
      <%= link_to "Take a Quiz", "/quiz", id: "take-quiz-button" %>
      <%= link_to "Sign Out", "/signout", id: "sign-out-button" %>
    </div>
  </div>

  <div id="tables-container">
    <div class="scores-container">
      <div class="table-title">Your Previous Scores</div>
      <table class="scores-table">
        <thead>
          <tr>
            <th>Date</th>
            <th>Score</th>
          </tr>
        </thead>
        <tbody>
          <% @previous_scores.each do |score| %>
            <tr>
              <td>No Date</td>
              <td><%= score %> / 20</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="scores-container">
      <div class="table-title">High Scores</div>
      <table class="scores-table">
        <thead>
          <tr>
            <th>Date</th>
            <th>Name</th>
            <th>Score</th>
          </tr>
        </thead>
        <tbody>
          <% @high_scores.each do |score| %>
            <tr>
              <td>No Date</td>
              <td><%= score[:name] %></td>
              <td><%= score[:number_correct] %> / 20</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>